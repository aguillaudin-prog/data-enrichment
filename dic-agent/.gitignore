data/*.sqlite
data/*.sqlite-journal
# Exception: the production seeded SQLite is bundled via git-lfs so Streamlit
# Cloud has the full nav/CIFP data on first boot.
!data/dic.sqlite

# Downloaded datasets (OurAirports, Natural Earth) — not committed
seeds/airports.csv
seeds/navaids.csv
seeds/ne_50m_admin_0_countries.geojson
seeds/*.zip
seeds/*.shp
seeds/*.dbf
seeds/*.shx
seeds/*.prj

# Curated/minimal seeds are tracked (aircraft_types.csv, minimal_*.csv, minimal_*.geojson)

__pycache__/
*.pyc
.venv/
out/

# Secrets — JAMAIS commiter
.env
.env.local
*.key
*.pem
.streamlit/secrets.toml

# OpenAIP cached data per region
seeds/openaip_*.json
seeds/openaip_*.geojson

# route_templates_*.json IS tracked (curated, no personal data)
